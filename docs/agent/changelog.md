# Change history

> Running change log, moved out of CLAUDE.md so it does not load on every task.

*2026-06-12 (later) — Prayer request + connect card, zero code. Two new form documents (`form-prayer-request`, notifications to the pastoral-care address, with an optional-name/optional-email shape and a "keep this between the pastor and me" checkbox; `form-connect-card`, to the office, with I-am/I-would-like-to selects) placed entirely through existing plumbing: a new `/prayer` page via the generic `page` type (hero + sectionForm block, in the sitemap automatically), the connect card appended to the worship (I'm New) page's `flexibleSections` between the reflection quote and the closing CTA, a "Prayer requests" row in the contact page's who-to-reach list, and a "Prayer Request" link in the footer's Connect column. Placement gotcha learned: `siteSettings.prayerUrl` only decorates the BUILT-IN footer columns; this site manages `footerColumns` in Sanity, so the link had to be added to the managed column (prayerUrl is also set for documentation/fallback). Seeded by `scripts/seed-connect-prayer.mjs` (idempotent, guarded patches). Verified on dev against live data: /prayer light/dark/mobile, worship section, footer + contact links. NOTE: forms deliver via Web3Forms and `PUBLIC_WEB3FORMS_KEY` must be set in Cloudflare for submissions to send (the renderer shows an inline notice instead of silently dropping when unset; applies to the three pre-existing forms too).*

*2026-06-12 — Sermon service records. Each sermon document gained a "Service record" Studio tab (the per-service archive): bulletin PDF, sermon text/notes PDF, hymns sung (title + hymnal number), choir and organ music (role/piece/composer, for the quartette choir + Austin organ), serving-in-worship roles (liturgist, organist, readers; the preacher stays the Speaker field), and a liturgical day string that joins the date in the detail page's meta line. The sermon page renders a "From this service" card (download pills + three lists) only when at least one record field is set, so existing sermons are unchanged. Verified with a dev-only mock (production dataset untouched), both themes + both viewports; empty state confirmed identical; production build clean; typegen + studio:deploy run. The schema is template-reusable: any church gets a sermon archive worthy of a historic congregation by just filling the fields.*

*2026-06-11 (later) — Highland Park Presbyterian emulation pass (research: `docs/research/hppres-emulation-2026-06-11.md`). Adopted from the named gold standard: (1) keyword gold emphasis extended from the home hero to every interior hero via a new `heroKeyword` field (page-singleton factory + about/faq/contact/events/sermons schemas, rendered by Hero.astro in lifted gold on the photo scrim; lowest-precedence flourish after rotator + script accent; seeded on 12 pages by `scripts/seed-hero-keywords.mjs`, which verifies the keyword appears in the live headline before writing). (2) New `ArchOrnament.astro` (triple Romanesque arch, gold stroke — our equivalent of HPP's stained-glass divider) replacing the FinalCta hairline and marking the worship reflection quote. (3) Sermons hub: featured card without artwork now renders a chapel-green typographic panel (arch mark + scripture/series) instead of a blank slab; optional `podcastAppleUrl`/`podcastSpotifyUrl` on sermonsPage render Apple/Spotify pills beside Watch Live when set. (4) Events index: a single upcoming event or special service renders centered at readable width instead of a half-empty 2-col grid. Deliberately not adopted: site search (new dependency), multi-gathering times table (single-service church), series-grid archive (2 sermons), with reasons in the research doc. typegen + studio:deploy run; production build clean; verified in both themes + both viewports.*

*2026-06-11 — UI polish pass + agent tooling. A 6-lens design audit (typography, color/dark, spacing, components, motion/a11y, art direction; 53 findings synthesized to 12) drove a token/class-level polish: pill-shaped `CtaLink` (was the lone rounded-rect CTA); theme-aware `--color-border-soft` (dark hairlines no longer glow) + a real dark surface ladder (`--card`/`--popover` lifted to `#2A231B` over `--muted #262019`); new `--gold-ink` token + lifted-gold literals so gold text passes AA on cards and chapel green (defends the a11y 100 the day a special service is published); global `:focus-visible` ring with a cream override on chapel surfaces; `text-wrap: balance` on headings + `text-pretty` on shared subheads; heading-to-grid spacing fixes (home/worship `mt-l`, beliefs `mt-section-md`); an AA sweep lifting small `/60`→`text-muted-foreground` and small `/70`→`/80`; FinalCta brought into the codified type voice (`leading-headline-tight`, `tracking-eyebrow`, `text-chapel-foreground`, gold hairline) and all eyebrow hairlines unified on gold; fixed the home hero stagger bug (6th child animated first — now `:nth-child(n+5)`/`(n+6)`); arch images framed in bronze (`ring-primary/40` + dark lifted-gold variant); new `.surface-chapel` gradient so green bands read lit, not flat; dark-mode photo dimming filter (scrimmed heroes excluded). Verified: production build clean, before/after screenshots both themes + both viewports, adversarial 3-lens review of the diff. New lasting artifacts: root `design.md` (one-file design brief for future visual work), `scripts/sanity-audit.mjs` + `/sanity-audit`, `/rebuild`, `/visual-verify` slash commands in `.claude/commands/`, OPERATIONS "Audit Sanity content" section + the three-layer content-mystery gotcha.*

*2026-05-30 — Forked from the Reid Design build; genericized to the ncs-astro-sanity-starter (core foundation + opt-in module library + bootstrap docs). Future projects start their own history from this entry.*

*2026-06-01 — Second Presbyterian Church of Chicago: full Sanity editability for a non-technical church team (PR #2, `feature/church-cms` → `master`). Configurable forms; announcement / worship-resource collections and enriched events + ministries; editable home + seasonal hero; every page's body copy and closing CTA editable via the inline-fallback pattern; FAQ page now reads the `faqItem` collection; a 13-block page builder with a background/media system on every page plus a generic `page` type at `/<slug>`. Studio polish: an in-Studio "How This Works" help center (12 guides), Studio themed to the brand (Bronze/Paper/Ink + Instrument Serif/Newsreader), editable header + footer navigation (`siteSettings.navItems` / `footerColumns`, with the built-in menus as fallback; mobile inherits the header), every document opens on the "All fields" tab, and a real favicon (bundled mark + `siteSettings.favicon` override). No destructive migrations. Full detail + decisions: `docs/church-cms-summary.md`.*

*2026-06-01 (later, on `master`) — Content audit + remediation. Audited Sanity-vs-hardcoded content and what the dataset actually had loaded (confirmed site + Studio share project `kz01wb83`). Loaded the empty `staffMember` (4, with uploaded headshots) and `ministry` (4 pillars) collections; filled siteSettings gaps; made the remaining hardcoded page lists editable (wedding FAQs + pricing, grow groups, serve ways, use-our-space uses, contact reasons, what-we-believe resources, home service band + rhythms) and pre-filled them; made the **street address** editable (`siteSettings.addressLine` + `cityStateZip`, wired everywhere incl. the LocalBusiness JSON-LD); fixed `give.astro` to read `siteSettings.giveUrl`. Removed the misleading Studio "Preview" tab (static site has no live draft preview) and corrected the help guide to the edit → Publish → rebuild model. New scripts: `audit-content`, `seed-staff`, `seed-ministries`, `seed-settings`, `seed-page-lists`.*

*2026-06-01 (later, on `master`) — Security headers. Expanded `public/_headers` into a full Content-Security-Policy (`object-src 'none'`, `base-uri 'self'`, a tight `script-src`, and the site's real img/media/frame/connect sources: Sanity, YouTube, Vimeo, Google Maps, Web3Forms, Cloudflare analytics), HSTS with `preload` (max-age 2y), and tightened `frame-ancestors` to `'self'` (the Studio preview was removed). Trusted Types intentionally skipped (would break the YouTube/Vimeo/Maps/analytics embeds). Addresses the unscored Lighthouse security recommendations; verified via `wrangler dev` with no CSP breakage on home + contact (cross-origin map + form).*

*2026-06-01 (later, on `master`) — Content-editability remediation (audit + fix). An audit (`docs/agent/content-audit-2026-06-01.md`, `scripts/audit-field-population.mjs`) found most page body-copy fields were empty in Sanity (Studio showed blank inputs while the site rendered the inline code fallbacks), plus a set of orphaned fields and truly-hardcoded items. Fixes, in phases: (A, schema) added an editable closing-CTA `finalCta` button to the shared church-page factory + the events/sermons/contact singletons so the bottom CTA button is editable on every page (it was a hardcoded literal in all 19 templates); removed dead orphan fields (`aboutPage` story\*, `contactPage` whatToExpect\*); fixed the 404 page's stale interior-designer defaults. (B, wiring) every `<FinalCta>` now reads `page.finalCta` (falling back to the prior literal); the 7 pages that hardcoded their `<title>`/meta description now read `seoTitle`/`seoDescription`; the home hero's long-orphaned `heroPrimaryCta`/`heroSecondaryCta` buttons are wired. (C, seed) re-ran `seed-page-lists.mjs` (its published patches had 404'd before the docs existed) and added `seed-page-copy.mjs` + `page-copy.json`, seeding 185 body-copy fields with copy extracted verbatim from the templates. Verified zero drift: a pre-seed vs post-seed build diff was byte-identical across all 29 HTML pages, and field population jumped (e.g. homePage 8→24, beliefsPage 4→35, worshipPage 4→28 of their fields set). No visual change; `typegen` + `studio:deploy` run. Still hardcoded (a later pass): in-body section images (home Welcome, about murals, worship Tiffany, music organ), the home events-teaser headings, contact phone/email/office-hours (from site.ts), and some event/sermon detail fields (cost, registrationLabel, contact, sermon image).*

*2026-06-01 (later, on `master`) — Docs: declared Sanity the single source of truth. Updated `CLAUDE.md` (a prominent "Content model" callout + corrected the "Safe to edit by hand" note so the `.astro` inline copy is described as a safety-net fallback, not the live content), `docs/agent/editor-vs-hardcoded.md` (leads with the principle + the single-sourced repeated values), `docs/agent/sanity.md`, `docs/agent/page-architecture.md`, `OPERATIONS.md`, and `docs/church-cms-summary.md`. The consistent message: every content field is populated so the Studio mirrors the live site; edit content in Studio (never the `.astro` fallbacks); repeated values (worship time, address/phone/email) are single-sourced in `siteSettings`.*

*2026-06-01 (later, on `master`) — Single-sourced the worship service time. The time previously appeared in ~15 independent spots (header, footer, home service band, home weekly rhythm, the "This Sunday" line, the worship-page time, the Google/JSON-LD opening hours which were hardcoded and uneditable, and a dozen CTA/SEO sentences). Now one canonical `siteSettings.worshipService` ({ time, day, startTime24, endTime24 }) is the only place the time lives. A new `src/lib/serviceTime.ts` helper derives every display format ("Sundays at 11am", "11am, every Sunday", "11am", "Every Sunday morning") and the JSON-LD hours from it, with the current values as defaults. Wired into Header, Footer, home service band + "This Sunday" line, worship "when we gather", and `schemas.ts` (the Google hours are now editable + correct). Removed the now-redundant per-page time fields (`siteSettings.serviceTimes`, home `serviceBand.worshipTime`, worship `gatherTime`/`gatherDay`). Reworded the ~12 CTA/SEO sentences that baked in "11am" to be time-agnostic ("join us for worship this Sunday") so the time never has to be repeated; `seed-service-time.mjs` seeds the canonical object + the reworded copy. Verified: the structured derivation is byte-identical to before, the reworded copy renders, the structured time + JSON-LD still read 11am/11:00, and no old "...at 11am" CTA phrasing remains. Left intentionally stating the time: the FAQ "What time is worship?" answer and the weekly-rhythms list (both editable content where naming the time is expected). typegen + studio:deploy run.*

*2026-06-01 (later, on `master`) — "Sanity is the source of truth" pass (no empty Studio fields while the site shows content). Seeded the header menu + footer columns (`siteSettings.navItems`/`footerColumns`, via `seed-nav.mjs`) so the menus live in Sanity instead of the code fallback; wired the 5 remaining pages that hardcoded their `<title>`/meta and seeded `seoTitle`/`seoDescription` on all 13 empty-SEO pages plus the home green keyword (`seed-seo.mjs`); and made every remaining hardcoded in-body button/link/heading editable (`ctaBlock`/string fields + `seed-e3.mjs`): home Welcome/service-band/See-the-Calendar buttons, about's 3 buttons, worship's in-body links + "Worship resources" heading, the music staff-credit link, food's two "Support" buttons, serve's "Learn more" label, the sermons "Watch Live"/"Watch on YouTube" labels + empty-state CTA, and the previously fully-hardcoded closing CTA on individual event + sermon detail pages (`detailFinalCta*` on the events/sermons singletons). All verified byte-identical (pre/post-seed build diffs of 0 pages at every step). The inline code fallbacks remain only as a safety net, so a field cleared by accident never blanks a section. typegen + studio:deploy run.*

*2026-06-01 (later, on `master`) — Seeded the CTA button fields too. The Phase B/C work wired + seeded the CTA text, but the ctaBlock BUTTON objects (home hero Primary/Secondary, and every page's closing finalCta) were left to fall back, so the Studio still showed those CTA fields blank while the site rendered the default buttons. `seed-ctas.mjs` now seeds each with its exact current label + link (the two link-derived buttons, home "Watch Online" -> watchUrl and give "Give Now" -> giveUrl, seeded with the current resolved URL so nothing drifts). Verified byte-identical (pre/post-seed build diff: 0 changed pages). Studio CTA fields now mirror the site.*

*2026-06-01 (later, on `master`) — Content-editability remediation, Phase D (the remaining hardcoded items). Made editable: the in-body section photos (home Welcome, about mural-feature + nave, worship children, music organ) via new per-page image fields, with the bundled photos uploaded to Sanity so the Studio shows the current image (template falls back to the bundled asset when unset); the home events-teaser eyebrow/headline/intro; contact phone/email/office-hours (now read `siteSettings` with `site.ts` as fallback; added `siteSettings.officeHours`); and the event-detail fields editors could fill but that never rendered (`registrationLabel`, `cost`, `contactName`/`contactEmail`, `audience`) plus the sermon-detail image (shown when a sermon has no video). New `seed-section-images.mjs` uploads the photos + seeds `officeHours`; `page-copy.json` gained the events-teaser copy. Verified: pre/post-seed build diff was byte-identical except the four pages whose section photo moved to the Sanity CDN (no text changed on any page), and those four were screenshot/DOM-checked to confirm the same images render from Sanity. `typegen` + `studio:deploy` run. Left intentionally hardcoded: small structural button labels/links (e.g. "Watch Online", in-body nav links) and the privacy-policy body (its `body` field is already editable; seeding the legalese as Portable Text risked markup drift for a rarely-edited page).*

*2026-06-01 (later, on `master`) — Arched media everywhere + nav tidy. Generalized the home hero slideshow into a reusable `src/components/ArchMedia.astro` (looping muted video > cross-fade slideshow > single image) whose advance script is multi-instance safe (every `[data-arch-slideshow]` gets its own timer; videos honor `prefers-reduced-motion`). Added a `sectionArchShowcase` page-builder block so editors can drop the same arched slideshow/video on any page (Studio radio picks photos vs video; video is an uploaded file or a direct .mp4/.webm link, with an optional poster; wired in `blocks.ts`, `Sections.astro` as shell-aware, and the `SECTION_MEMBERS` GROQ). Gave the home hero its own optional `heroVideo` / `heroVideoUrl` / `heroVideoPoster` (video wins over the photo slideshow; seasonal hero still overrides as a single image). Fixed the LCP softness from the first slideshow pass: the constant Ken Burns zoom magnified the old 1000px image cap, so arched media now requests width 1600 / quality 76. Nav: replaced the main-header `Give` item with `Contact` (→ /contact) and added `Contact` to the footer Connect column (the utility-bar Give quick-link stays). Removed the now-superseded `HeroArchSlideshow.astro`. Schema change, so `typegen` + `studio:deploy` were re-run. Verified: build clean, home hero crisp + advancing (light/dark, desktop/mobile, 0 console errors), and the new block renders both a slideshow and a video (poster confirms the arch fill) via a throwaway test page.*

*2026-06-01 (later, on `master`) — Home hero slideshow. The home page arched photo frame can now cross-fade through multiple images instead of showing one. New `src/components/HeroArchSlideshow.astro` reuses the global `.hero-slideshow` / `.hero-slide` CSS and the advance script from `HeroBackground.astro` (4.5s step, ~1.5s cross-fade, slow Ken Burns zoom), but without the full-bleed darkening overlay since it sits inside the framed photo. `index.astro` switches the arched frame to the slideshow whenever the Home singleton's `heroImages` array holds 2+ images and no seasonal hero is active; otherwise it renders the single image as before. Seeded `homePage.heroImages` with 10 distinct church photos via `scripts/seed-hero-images.mjs` (caught that several `hero-*.webp` files in `src/assets` are byte-identical placeholder copies, which Sanity dedupes by content hash, and chose 10 unique images so no slide repeats). Editors add/remove/reorder slides in the Studio. Reduced-motion users get the first slide, static. Verified: build clean, 10/10 distinct assets in the dataset, slideshow advances with zero console errors in light + dark, desktop + mobile.*
